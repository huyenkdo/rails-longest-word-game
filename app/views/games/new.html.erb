<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <div class="container">
    <h1>New game</h1>
    <div class="hstack gap-3 mt-4">
      <% @letters.each do |letter| %>
      <div class="bg-info text-white px-3 py-2 fs-3 fw-bold shadow" style="width: 50px; text-align: center"><%= letter %></div>
      <% end %>
    </div>
    <p class="fs-6 fw-light my-3">What is the longest word you can find?</p>
    <form action="/score" method="post" data-turbo="false" class="input-group">
      <input type="text" class="form-control" name="word">
      <button class="btn btn-outline-secondary rounded-end" type="button">Play</button>
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <%= hidden_field_tag :letters, @letters %>
    </form>
  </div>
</body>
</html>
